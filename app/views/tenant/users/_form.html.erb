<%= form_with(model: tenant_user, local: true) do |form| %>
  <% if tenant_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tenant_user.errors.count, "error") %> prohibited this tenant_user from being saved:</h2>

      <ul>
      <% tenant_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
